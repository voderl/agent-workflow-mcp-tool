{
  "name": "agent-workflow-mcp-tool",
  "version": "1.0.1",
  "description": "Code controlled agent workflow. Agent lies, code not.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "_publish": "npx tsc && npm publish --registry https://registry.npmjs.org",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "homepage": "https://github.com/voderl/agent-workflow-mcp-tool",
  "keywords": [
    "agent",
    "workflow",
    "mcp"
  ],
  "files": [
    "dist/**/*",
    "src/**/*",
    "tsconfig.json",
    "README.md"
  ],
  "author": "voderl",
  "license": "MIT",
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.16.3",
    "@types/node": "^18.19.122",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.2",
    "zod": "^4",
    "@anthropic-ai/claude-agent-sdk": "^0.1.75"
  }
}
